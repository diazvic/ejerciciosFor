// Alumna más grande

// Dado un array bidimensional compuesto de dos arrays de 5 elementos cada uno, donde el primero esta compuesto de nombres y el segundo de edades, retornar el nombre de la persona con la mayor edad.

const nombresEdades = [
	["Euge", "Guadi", "Fran", "Santi", "Moni"],
	["25", "45", "10", "8", "96"],
];
mostrarPersonaMayor = (nombresEdades) => {
	for (let i = 0; i < nombresEdades.length; i++) {
		const nombre = nombresEdades[i];
		console.log(nombre);
		for (let j = 0; j < nombre.length; j++) {
			if (nombre[j]) {
			}
		}
	}
};
